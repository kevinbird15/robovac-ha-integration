name: 'Setup Python Environment'
description: 'Checks out code, sets up Python, Task, uv, and installs dev dependencies'

inputs:
  python-version:
    description: 'Python version to set up'
    required: false
    default: '3.13'
  dev-dependencies:
    description: 'Install dev dependencies'
    required: false
    default: 'false'
  dependencies:
    description: 'Install dependencies'
    required: false
    default: 'false'

runs:
  using: "composite"
  steps:
    - name: Set up Python
      uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6
      with:
        python-version: ${{ inputs.python-version }}

    - name: Install Taskfile
      uses: arduino/setup-task@v2
      with:
        version: 3.x
        repo-token: ${{ github.token }} # Use github.token in composite actions

    - name: Install uv
      # Using the specific commit SHA you had, as it might be intentional over a simple vX tag
      uses: astral-sh/setup-uv@4e1e303f7dafb1a3ec7770a507052543f593ad96 # From user's ci.yml

    - name: Install Python dependencies
      shell: bash
      run: task install-dev
      if: ${{ inputs.dev-dependencies == 'true' }}

    - name: Install Python dependencies
      shell: bash
      run: task install
      if: ${{ inputs.dependencies == 'true' }}
